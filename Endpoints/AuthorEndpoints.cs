namespace EndPoints
{
    public class AuthorEndpoints
    {
        public IResult GetAuthors()
        {
            return Results.Ok();
        }

        public IResult GetAuthor()
        {
            return Results.Ok();
        }

        public IResult AddAuthor()
        {
            return Results.Ok();
        }
    }
}
